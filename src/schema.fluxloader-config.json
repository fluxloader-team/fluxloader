{
	"gamePath": {
		"type": "string",
		"description": "Path to the game executable",
		"default": ".",
		"display": "pathdir"
	},
	"modsPath": {
		"type": "string",
		"description": "Directory where mods are stored",
		"default": "./mods",
		"display": "pathdir"
	},
	"modsEnabled": {
		"type": "object",
		"description": "Which mods are enabled / disabled",
		"hidden": true,
		"default": {}
	},
	"isLoadOrderManual": {
		"type": "boolean",
		"description": "Whether the load order is manually specified or automatically determined",
		"default": false
	},
	"loadOrder": {
		"type": "array",
		"description": "Array specifying the order in which mods should be loaded",
		"hidden": true,
		"default": []
	},
	"loadIntoManager": {
		"type": "boolean",
		"description": "Whether to startup into the mod manager or directly into the game",
		"default": true,
		"hidden": true
	},
	"closeGameWithManager": {
		"type": "boolean",
		"description": "Close the game when the mod manager is closed",
		"default": true
	},
	"ignoreUnhandledExceptions": {
		"type": "boolean",
		"description": "Ignore unhandled exceptions instead of crashing",
		"default": false
	},
	"logging": {
		"logToFile": {
			"type": "boolean",
			"description": "Enable logging to a file",
			"default": true
		},
		"logToConsole": {
			"type": "boolean",
			"description": "Enable logging to the console",
			"default": true
		},
		"consoleLogLevel": {
			"type": "dropdown",
			"description": "Minimum log level for console output",
			"default": "info",
			"options": ["debug", "info", "warn", "error"]
		},
		"fileLogLevel": {
			"type": "dropdown",
			"description": "Minimum log level for file output",
			"default": "debug",
			"options": ["debug", "info", "warn", "error"]
		},
		"latestLogFilePath": {
			"type": "string",
			"description": "Path to the latest log file",
			"default": "fluxloader-latest.log",
			"display": "pathfile"
		},
		"previousLogFilePath": {
			"type": "string",
			"description": "Path to the previous log file",
			"default": "fluxloader-previous.log",
			"display": "pathfile"
		}
	},
	"game": {
		"openDevTools": {
			"type": "boolean",
			"description": "Open developer tools for the game window",
			"default": false
		},
		"enableDebugMenu": {
			"type": "boolean",
			"description": "Enable the in-game debug menu",
			"default": false
		},
		"debugMenuZoom": {
			"type": "number",
			"description": "Zoom level for the debug menu UI",
			"default": 0.8,
			"min": 0.1,
			"max": 2.0,
			"step": 0.1
		},
		"disableMenuSubtitle": {
			"type": "boolean",
			"description": "Disable the subtitle in the main menu",
			"default": false
		}
	},
	"manager": {
		"autoConnect": {
			"type": "boolean",
			"description": "Automatically connect to mod database on startup",
			"default": true
		},
		"defaultSchemaFallback": {
			"type": "boolean",
			"description": "When a config value is invalid, try to use the default value from the schema instead of erroring",
			"default": true
		},
		"minResizerSize": {
			"type": "number",
			"description": "The minimum size, in pixels, that sidebars can shrink to (e.g. mod info panel)",
			"default": 200,
			"min": 0
		},
		"maxSearchSidebarSize": {
			"type": "number",
			"description": "The maximum size, in pixels, the left search sidebar can expand to",
			"default": 350,
			"min": 0
		},
		"maxModInfoSidebarSize": {
			"type": "number",
			"description": "The maximum size, in pixels, the right modinfo sidebar can expand to",
			"default": 800,
			"min": 0
		},
		"updateOS": {
			"type": "dropdown",
			"description": "Which file to download when downloading updates",
			"default": "None",
			"options": ["None", "Windows x64 Portable", "Windows arm64 Portable", "Windows legacy Portable", "macOS x64 Zip", "macOS arm64 Zip", "Linux x86_64 AppImage", "Linux arm64 AppImage", "Linux arm64 deb"]
		},
		"openDevTools": {
			"type": "boolean",
			"description": "Open developer tools for the mod manager window",
			"default": false
		}
	}
}
